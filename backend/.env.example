# Copy to .env and fill in your local PostgreSQL credentials.
PORT=5000
CORS_ORIGIN=http://localhost:3000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=jaleedkhan
DB_NAME=smart_pay_solution

# Auto-creates tables from the entities on boot. Turn off once you add migrations.
DB_SYNCHRONIZE=true
